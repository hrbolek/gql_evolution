
[pytest]
asyncio_mode = auto
addopts = --cov=src --cov-report=term-missing --cov-report=html
testpaths = tests
python_files = test_*.py
markers =
    explicit: manually maintained explicit service and GraphQL contract tests

